#content.content
   .container.bg-white.p-t-10
      h3.m-b-15 Notify the person
      form.form-horizontal(method="POST", action="{{action}}", novalidate name="form")
         .form-group(ng-class="{'has-error': (form.$submitted || form.email.$touched) && (form.email.$error.required || form.email.error.email)}")
            label.col-md-3.control-label Email address
            .col-md-9
               input.form-control(type="email", name="email", ng-model="email", required)
         input.hidden(name="place", ng-model="placeId", required)
         .form-group
            label.col-md-3.control-label
            .col-md-9
               input.btn.btn-sm.btn-primary(type="button", ng-click="submitForm()", value="Send")